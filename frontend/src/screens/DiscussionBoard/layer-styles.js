import { defineLayerStyles } from "@chakra-ui/react";

const layerStyles = defineLayerStyles({
  post: {
    description: "post styles",
    value: {
      background: "white.50",
      width: "300px",
    },
  },
});
